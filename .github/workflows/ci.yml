name: CI

on:
  push:
  pull_request:

# Cancel superseded runs on the same branch/PR to save CI minutes.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # backend/src/server.js and backend/test/**/*.test.js use node:sqlite and
  # node:test (node --test), both of which require Node 24.
  NODE_VERSION: '24'

jobs:
  backend:
    name: Backend (test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run syntax check (lint)
        run: npm run lint

      # No .env file is created here on purpose: every entry in
      # CONFIG_SCHEMA.*.required in backend/src/config.js is empty, and the
      # 122 backend/test/*.test.js tests save/restore any env var they touch
      # (see auth.test.js, secret-store.test.js) — the suite must pass with
      # a completely empty environment.
      - name: Run tests
        run: npm test

  gateway:
    name: Agent gateway (test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: idp-agent-gateway
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: idp-agent-gateway/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run syntax check
        run: npm run check:syntax

      # The suite starts real listeners on 127.0.0.1 with port 0, so it needs
      # no fixed ports and no network beyond loopback.
      - name: Run tests
        run: npm test
