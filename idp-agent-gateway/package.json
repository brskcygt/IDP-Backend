{
  "name": "idp-agent-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "IDP Java agent bağlantıları ve komut yönlendirmesi için bağımsız gateway",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check:syntax": "node --check src/server.js && node --check src/gateway.js"
  },
  "engines": { "node": ">=22" },
  "dependencies": { "ws": "^8.18.3" }
}
